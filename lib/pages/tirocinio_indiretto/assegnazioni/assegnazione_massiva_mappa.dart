import 'package:flutter/material.dart';

class AssegnazioneMassivaMappa extends StatelessWidget {
  const AssegnazioneMassivaMappa({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return 

            //TODO: MAPPA
            SliverToBoxAdapter(child: Text("MAPPA"));
  }
}